<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="resource.DataMapper">
	<!--0309 jung selectwriter mapper -->
	<select id="csickData1" parameterType="string"
		resultType="model.vo.GoodfoodVO">
		select good_food_cd, good_food_name, good_food_img from goodfood where sick_cd= #{sick_cd}
	</select>
	
		<select id="csickData2" parameterType="string"
		resultType="model.vo.BadfoodVO">
		select bad_food_cd, bad_food_name, bad_food_img from badfood where sick_cd= #{sick_cd}
	</select>
	
</mapper>
